.search
  = link_to 'Add New Candidate', new_candidate_path, :class => "button add"

%table#candidates.standard
  %caption Candidates
  %thead
    %tr
      - %w(Name Phone Email Github Twitter Skills Resume Actions).each do |th|
        %th= th
  %tbody
    = render candidates
